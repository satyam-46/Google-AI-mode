"""ADK lifecycle callbacks for observability (stubs)."""

def before_model_callback(context, request):
    # Log minimal metadata
    return None


def after_model_callback(context, response):
    # Log minimal metadata
    return None
